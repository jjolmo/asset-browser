{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Asset Browser",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "fs:default",
    "core:window:allow-set-title",
    "core:window:allow-title",
    "core:window:allow-show"
  ]
}
